# GreenTechPlatform

## 协作纪律（重要）

- 任何代码改动（新增/删除/修改文件、改动逻辑/样式/接口）必须先征求我的同意，并先与我确认改动方案后再动手修改。

## 开发约定（SQL / 数据脚本）

- 所有数据迁移、批量导入、清洗相关的 SQL 脚本统一放在 `data/` 目录。
- 根目录禁止新增零散的 `.sql` 迁移脚本，如需新增请在 `data/` 下创建对应文件并在 `docs/README.md` 中补充说明。
